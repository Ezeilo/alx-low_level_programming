#include <stdio.h>
/**
 * main- entry point for the program.
 * 
 * Return: 0 if no error, returns non-zero if errors are present.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
